# Sphere light

A spherical area light for the `RayTracer` renderer. Add one or more to a scene to illuminate it, controlling position, color, intensity, and radius. Color values are not restricted to `[0, 1]`, allowing HDR lighting.

```{eval-rst}  
.. autoclass:: genesis.options.renderers.SphereLight
    :show-inheritance:
```
